# Copy to `.env.local` (never commit `.env.local`). Restart `npm run dev` after changes.
#
# Apply SQL in order: supabase/migrations (see supabase/MIGRATION_ORDER.txt).
# Smoke test: npm run verify:supabase

# --- Supabase (Dashboard → Project Settings → API) ---
NEXT_PUBLIC_SUPABASE_URL=https://YOUR_PROJECT_REF.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your_anon_or_publishable_key
# Server only — never NEXT_PUBLIC_. Used for Paystack verify, order writes, sitemap product URLs.
SUPABASE_SERVICE_ROLE_KEY=your_service_role_secret

# --- Paystack ---
# Select active payment provider (`paystack` default, `moolr` reserved for future integration).
PAYMENT_PROVIDER=paystack
NEXT_PUBLIC_PAYMENT_PROVIDER=paystack
NEXT_PUBLIC_PAYSTACK_PUBLIC_KEY=
PAYSTACK_SECRET_KEY=
# Configure Paystack dashboard webhook URL to:
# https://<your-domain>/api/paystack/webhook

# --- WhatsApp click-to-chat (digits only, no +) ---
NEXT_PUBLIC_WHATSAPP_NUMBER=

# --- Optional: canonical site URL for sitemap, robots, Open Graph ---
# NEXT_PUBLIC_SITE_URL=https://your-domain.com

# --- Optional: footer social links (full https URLs to your profiles) ---
# NEXT_PUBLIC_SOCIAL_INSTAGRAM_URL=https://instagram.com/yourhandle
# NEXT_PUBLIC_SOCIAL_FACEBOOK_URL=https://facebook.com/yourpage
# NEXT_PUBLIC_SOCIAL_X_URL=https://x.com/yourhandle
# NEXT_PUBLIC_SOCIAL_TIKTOK_URL=https://www.tiktok.com/@yourhandle
# NEXT_PUBLIC_SOCIAL_YOUTUBE_URL=https://www.youtube.com/@yourchannel
# NEXT_PUBLIC_SOCIAL_SNAPCHAT_URL=https://www.snapchat.com/add/yourhandle
# NEXT_PUBLIC_SOCIAL_EMAIL=hello@your-domain.com
